<!--
The changelog is maintained at the repository root so it is readable from a
clone and from GitHub's file view. This page includes that same file rather
than restating it, so the two can never drift.
-->

--8<-- "CHANGELOG.md"
